# Git hook runner — v. docs/07-tech-architecture.md §6.
# Instalacija (jednom, nakon `npm i`): npx lefthook install
#
# Web dio (`src/`, buduci `web/`) nema eslint/prettier konfigurisan još —
# hook za to se dodaje kad taj toolchain stvarno postoji (docs/07 §4),
# ne prije, da se ne blokira svaki commit na alatu koji ne radi.
pre-commit:
  parallel: true
  commands:
    # Prvo identitet, pa tek onda sve ostalo — firmski mail u commitu se poslije
    # moze samo prepisati (rewrite svih SHA-ova), ne obrisati. v. commit 87f0aff.
    git-identitet:
      # skip_empty: false — provjera ne gleda fajlove nego identitet, pa mora
      # ici i kad u commitu nema nijednog fajla koji drugi hookovi prate.
      skip_empty: false
      run: ./tool/check_git_identity.sh

    dart-format:
      glob: "{apps,packages}/**/*.dart"
      run: dart format --set-exit-if-changed {staged_files}
      stage_fixed: true
