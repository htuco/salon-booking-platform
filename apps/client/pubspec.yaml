name: client
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

resolution: workspace

environment:
  sdk: ^3.13.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  # Logo i cover salona se ucitavaju sa svakim otvaranjem ekrana; bez kesa to je
  # isti download na svakom hladnom startu i vidljiv prazan okvir umjesto brenda.
  cached_network_image: ^3.4.1
  cupertino_icons: ^1.0.8
  # Ulazna animacija success ekrana. Kratka je namjerno — `AppDuration` gornju granicu
  # drzi na 300 ms. `confetti` je bio dodan pa uklonjen: DoD ga trazi "kao u prototipu",
  # a prototip (`07-zahtjev-poslan.png`) konfeta nema, i slavlje nad nepotvrdjenim
  # zahtjevom je obecanje koje ekran ne smije dati.
  flutter_animate: ^4.5.2
  core_api:
    path: ../../packages/core_api
  core_domain:
    path: ../../packages/core_domain
  core_ui:
    path: ../../packages/core_ui
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  flutter_web_plugins:
    sdk: flutter
  go_router: ^14.6.2
  intl: any
  # Verzija i build broj na ekranu „O aplikaciji" (5n). Konstanta u kodu bi zastarila prvim
  # buildom koji je niko ne ažurira — a ekran koji tvrdi pogrešnu verziju je ekran zbog
  # kojeg prijava greške ide na krivi build.
  package_info_plus: ^8.1.2
  supabase_flutter: ^2.8.0
  # `tel:`, `mailto:`, mape i profili na drustvenim mrezama sa home ekrana
  # (`docs/02 §3`). Bez ovoga je sekcija kontakta spisak teksta koji se ne moze pozvati.
  url_launcher: ^6.3.1
  lucide_icons_flutter: ^3.1.19

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # `flutter pub get` i build pokrecu gen-l10n; konfiguracija je u l10n.yaml.
  generate: true

  # Placeholder ploce iz handoffa (`prototype/ui/assets/ph*.png`). **Koristi ih samo
  # `demo_main.dart`**, da screenshot bude 1:1 sa mockupom; produkcijski build ostavlja
  # okvir prazan dok ne stignu prave fotografije salona. Lazne fotografije u storeu su
  # gore od praznog okvira.
  assets:
    - assets/demo/

  # Tipografija iz `prototype/ui/SPEC.md`: DM Serif Display za naslove, Archivo za tijelo.
  # **Pakuju se uz aplikaciju, ne ucitavaju se sa mreze** — handoff to izricito trazi, i
  # font koji stigne preko mreze znaci prvi frame u pogresnom pismu (ili bez njega).
  #
  # Archivo je **varijabilni** font (osi `wdth`/`wght`); staticki rezovi ne postoje u
  # google/fonts izvoru. Tezine se zato biraju kroz `FontVariation('wght', …)` u
  # `core_ui` tipografiji, ne kroz zasebne fajlove po tezini.
  #
  # Licence stoje uz fajlove (`OFL-*.txt`) — oba su SIL Open Font License 1.1.
  fonts:
    - family: DM Serif Display
      fonts:
        - asset: assets/fonts/DMSerifDisplay-Regular.ttf
    - family: Archivo
      fonts:
        - asset: assets/fonts/Archivo[wdth,wght].ttf

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
